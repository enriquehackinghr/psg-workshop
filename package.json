{
  "name": "meridiany",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "server",
    "client",
    "widget"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=server & npm run dev --workspace=client",
    "dev:server": "npm run dev --workspace=server",
    "dev:client": "npm run dev --workspace=client",
    "preview:tunnel": "npx localtunnel --port 5173",
    "build": "npm run build --workspace=client && npm run build --workspace=widget && npm run build --workspace=server",
    "start": "npm run start --workspace=server",
    "ingest": "npm run ingest --workspace=server"
  },
  "engines": {
    "node": ">=20"
  }
}
